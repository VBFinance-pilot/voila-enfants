# Supabase (client-side, VITE_ prefix required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Google Analytics 4 (client-side, VITE_ prefix required)
# Only loads in production builds; leave unset locally to disable tracking in dev.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# SMTP (server-side only - set in Vercel Dashboard)
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password
CONTACT_EMAIL_TO=contact-voilaJP@protonmail.com
